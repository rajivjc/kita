import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Sign In — SOSG Running Club' }

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return children
}
