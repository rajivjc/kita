import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Set Up — SOSG Running Club' }

export default function SetupLayout({ children }: { children: React.ReactNode }) {
  return children
}
